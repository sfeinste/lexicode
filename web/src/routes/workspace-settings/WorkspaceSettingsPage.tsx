import { Placeholder } from "../Placeholder";

export function WorkspaceSettingsPage() {
  return (
    <Placeholder
      title="Workspace settings"
      note="Members, integrations, defaults and the audit log arrive with S08 and later stories."
    />
  );
}
